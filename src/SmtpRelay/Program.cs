using Microsoft.Extensions.DependencyInjection;using Microsoft.Extensions.Hosting;using Serilog;namespace SmtpRelay{public static class Program{public static void Main(string[]a){var cfg=Config.Load();Logging.Configure(cfg);Host.CreateDefaultBuilder(a).UseWindowsService(o=>o.ServiceName="SMTPRelayService").UseSerilog().ConfigureServices(s=>s.AddHostedService<Worker>()).Build().Run();}}}
