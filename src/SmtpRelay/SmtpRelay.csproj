<Project Sdk="Microsoft.NET.Sdk.Worker">
  <PropertyGroup>
    <!-- Build as a standalone Windows service -->
    <TargetFramework>net8.0-windows</TargetFramework>
    <UseWindowsService>true</UseWindowsService>
    <SelfContained>true</SelfContained>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <PublishSingleFile>true</PublishSingleFile>
    <!-- Trimmed publish can break JSON, so omit PublishTrimmed here -->
  </PropertyGroup>

  <ItemGroup>
    <!-- SMTP protocol engine -->
    <PackageReference Include="SmtpServer" Version="11.0.0" />

    <!-- For reading the transaction payload -->
    <PackageReference Include="MailKit" Version="4.1.0" />
    <PackageReference Include="MimeKit" Version="4.4.0" />

    <!-- Structured logging -->
    <PackageReference Include="Serilog" Version="4.2.0" />
    <PackageReference Include="Serilog.Extensions.Hosting" Version="9.0.0" />
    <PackageReference Include="Serilog.Sinks.File" Version="7.0.0" />

    <!-- CIDR/Range parsing -->
    <PackageReference Include="IPAddressRange" Version="3.0.0.1" />
  </ItemGroup>
</Project>
